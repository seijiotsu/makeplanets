/*
 * Control all the stars
 */

/*
var NUM_STARS = 200;
var stars = [];

for (var i = 0; i < NUM_STARS; i++) {
  var star = document.createElement("div");
  star.className = "star";
  
  var size = 1 + Math.random()*2;
  star.style.width = size + "px";
  star.style.height = size + "px";
  star.style.left = Math.random() * (window.innerWidth - size*2) + "px";
  star.style.top = Math.random() * (window.innerHeight - size * 2) + "px";
  star.style.zIndex = -9999;
  document.body.appendChild(star);
  stars.push(star);
}
*/
